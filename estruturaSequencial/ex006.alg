algoritmo "05042022ex006"

var
   r, area : real
inicio
      escreval("informe o raio do circulo para poder calcular sua area :")
      leia(r)
      area <- 3.14 * (r*2)
      escreva("A area do circulo que tem como raio", r, " é :", area, " centimetros quadrados")

fimalgoritmo
