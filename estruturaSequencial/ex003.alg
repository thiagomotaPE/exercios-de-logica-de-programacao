algoritmo "04042022ex003"

var
  n1, n2, soma : inteiro
inicio
      escreval("Digite um numero:")
      leia(n1)
      escreval("Digite outro numero:")
      leia(n2)
      soma <- n1 + n2
      escreval("A soma entre os dois numeros informados é :", soma)
fimalgoritmo
